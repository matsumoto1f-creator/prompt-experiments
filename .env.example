# Copy to .env. Never commit real values.

# Only needed with --provider anthropic / PROMPT_EXPERIMENTS_PROVIDER=anthropic.
ANTHROPIC_API_KEY=

PROMPT_EXPERIMENTS_DB=experiments.db
PROMPT_EXPERIMENTS_PROVIDER=mock
